{
  "name": "gh-action-docker-state",
  "version": "1.0.0",
  "main": "./src/index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && ncc build -m ./src/index.ts -o dist"
  },
  "devDependencies": {
    "@actions/cache": "^1.0.7",
    "@actions/core": "^1.4.0",
    "@types/node": "^15.12.4",
    "@vercel/ncc": "^0.28.6",
    "typescript": "^4.3.4"
  }
}
