{
  "name": "gh-action-docker-state",
  "version": "1.0.0",
  "main": "./src/index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && ncc build -m ./src/index.ts -o dist"
  },
  "devDependencies": {
    "@actions/cache": "^3.0.4",
    "@actions/core": "^1.10.0",
    "@types/node": "^18.8.4",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.8.4"
  }
}
