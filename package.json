{
  "name": "gh-action-docker-state",
  "version": "1.0.0",
  "main": "./src/index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && ncc build -m ./src/index.js -o dist"
  },
  "dependencies": {
    "@actions/cache": "^1.0.6",
    "@actions/core": "^1.2.6"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0"
  }
}
