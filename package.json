{
  "name": "gh-action-docker-state",
  "version": "1.0.0",
  "main": "./src/index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && ncc build -m ./src/index.ts -o dist"
  },
  "devDependencies": {
    "@actions/cache": "^1.0.8",
    "@actions/core": "^1.9.1",
    "@types/node": "^17.0.13",
    "@vercel/ncc": "^0.33.1",
    "typescript": "^4.5.5"
  }
}
